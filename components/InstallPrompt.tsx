import React from 'react';

// Этот компонент больше не используется.
// Функция "Добавить на экран «Домой»" теперь доступна через
// стандартное меню Telegram (три точки в правом верхнем углу).
// Telegram автоматически добавляет эту опцию в меню, если приложение
// правильно настроено (manifest.json).

const InstallPrompt: React.FC = () => {
    return null;
};

export default InstallPrompt;
